#include<stdio.h>

int main()
{
	int a=10;
	printf("%d",a);
	
}


		memset(arr,0,20);
		while('\n' != getchar())
		{
			;
		}
		fgets(arr,19,file2);
		len = strlen(arr);
		if('\n'==arr[len-1])
		{
			arr[len-1]='\0';
		}
		fputs(arr,file3);

}


}
